-- KPI Tổng kim ngạch (VND) — chart: KPI
-- Cần 1 trục ROWS mới hiển thị; dùng year.Members (không hard-code) — dashboard filter thu hẹp.
SELECT {[Measures].[TradeValue_VND]} ON COLUMNS,
  NON EMPTY [dim_time].[year].Members ON ROWS
FROM [Vietnam_Trade_Analysis_Cube]
